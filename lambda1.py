# 🔹 기본 개념 (from chat_gpt)

# 일반 함수는 이렇게 만듭니다:

# def add(x, y):
#     return x + y

# 람다 함수는 같은 기능을 이렇게 짧게 쓸 수 있습니다 👇

# add = lambda x, y: x + y
    # 즉, lambda 인자: 표현식
    # 형태로 작성되며, return 키워드는 쓰지 않습니다.
    # 표현식의 결과가 자동으로 반환(return)됩니다.

# 🔹 예시들
# ✅ 1. 기본 사용
# square = lambda x: x * x
# print(square(5))   # 출력: 25

# ✅ 2. 여러 인자
# add = lambda a, b: a + b
# print(add(3, 7))   # 출력: 10

# ✅ 3. 조건식 사용
# check = lambda x: "짝수" if x % 2 == 0 else "홀수"
# print(check(4))   # 출력: 짝수

# 🔹 람다가 자주 쓰이는 곳
# ✅ 1. 정렬 (key 인자)
data = [(1, 5), (2, 3), (3, 1)]
data.sort(key=lambda x: x[1])
print(data)  # [(3, 1), (2, 3), (1, 5)]

# ✅ 2. map(), filter(), reduce() 와 함께
nums = [1, 2, 3, 4, 5]

# 각 원소 제곱
squared = list(map(lambda x: x**2, nums))
print(squared)  # [1, 4, 9, 16, 25]

# 짝수만 추출
even = list(filter(lambda x: x % 2 == 0, nums))
print(even)  # [2, 4]

# 🔹 주의점

# 한 줄짜리 간단한 함수에만 사용하세요.
# 복잡한 로직은 def로 정의하는 게 가독성이 좋습니다.

# return 문이나 여러 줄 코드, 예외 처리 등은 쓸 수 없습니다.